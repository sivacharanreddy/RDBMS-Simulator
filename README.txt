RDBMS Simulator
================

This project showcases the implementation of simple relational database in C, capable of processing the following commands:

• CREATE 
• DROP
• INSERT
• UPDATE with/without WHERE
• DELETE with/without WHERE
• SELECT with/without WHERE, AGGREGATE functions
• BACKUP 
• RESTORE

- In addition, a transaction log file is maintained for all the DDL and DML commands